import { StyleSheet } from "react-native";

const shadowStyle = StyleSheet.create({
  shadow: {
    shadowColor: "#000000",
    shadowRadius: 2,
    shadowOpacity: 0.5,
    elevation: 5,
  },
});

export default shadowStyle;
